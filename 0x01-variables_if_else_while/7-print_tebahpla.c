#include <stdio.h>

/**
 *main - print alphabet reversed using putchar
 *Return: Always 0 (Success)
 */

int main(void)
{
	char alpha = 'z';

	while(alpha >= 0)
	{
		putchar(alpha);
		alpha--;
	
	}

	putchar('\n);

	return (0)

}

